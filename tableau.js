// Manip de tableau

let groupeNom = ["Saïd", "Pomme", "Poire"];

// console.log("le nom afficher : " + groupeNom[0]);

// Mise en place d'un objet : client

let client = {
    nom : "Cazers",
    prenom : "Dev",
    numeroClient : 123,
    addresse : "15 avenue de l'Arbre",
    celibataire : false
};

console.log("Nom : " + client.nom);
console.log("Prenom : " + client.prenom);

// console.log(client["nom"]);